name: Check Inter-Crate Dependencies
permissions:
  contents: read

on:
  pull_request:
    paths:
      - '**/*.md'

jobs:
  check-inter-crate-dependencies:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipping dependency check for markdown-only changes"
