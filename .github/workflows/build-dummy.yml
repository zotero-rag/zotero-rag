name: Build pdftools and rag crates

on:
  pull_request:
    paths:
      - '**/*.md'
      - 'zqa/**'

jobs:
 build:
   runs-on: ubuntu-latest
   steps:
     - run: echo "Skipping build for markdown-only changes"

